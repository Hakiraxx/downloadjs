# Environment Configuration
NODE_ENV=development
# NODE_ENV=production (for hosting)

# Server Configuration
# For local development
PORT=3001
HOST=localhost

# For hosting (will be auto-detected)
# PORT=8080 (set by hosting platform)
# HOST=0.0.0.0 (set automatically for hosting)

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security (optional)
SESSION_SECRET=your-super-secret-session-key-here

# Platform Detection (auto-set by hosting platforms)
# DYNO=1 (Heroku)
# RAILWAY_ENVIRONMENT=production (Railway)
# RENDER=true (Render)
# VERCEL=1 (Vercel)
